# DVC Workflow

## Dataset Tracking and Versioning

DVC (Data Version Control) is used to track and version datasets while Git tracks the DVC metadata files.

### DVC Workflow

1. Initialize DVC in the Git repository using:

```bash
dvc init